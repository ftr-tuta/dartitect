name: dartitect_jobs
description: Provider-neutral bounded headless job execution contracts for Dart applications.
version: 1.1.0-rc.2
publish_to: none
repository: https://github.com/ftr-tuta/dartitect
issue_tracker: https://github.com/ftr-tuta/dartitect/issues
topics: [jobs, background, lifecycle, dart]
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  dartitect:
    git:
      url: https://github.com/ftr-tuta/dartitect.git
      path: packages/dartitect
      tag_pattern: 'v{{version}}'
    version: 1.1.0-rc.2
dev_dependencies:
  test: ^1.26.3
