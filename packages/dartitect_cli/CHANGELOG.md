# Changelog

## 1.0.0-rc.2

- Promote the lockstep candidate cohort without public Dart API changes and
  bind release validation to the RC.2 source and evidence matrix.

## 1.0.0-rc.1

- Assemble the reviewed lockstep 1.0 release candidate without public API
  changes from the development cohort.

- Resolve `DartitectValue` annotations by semantic element identity, add
  deterministic fault injection across every generator transaction phase, and
  recover pre-journal staging residue safely.
- Replace consumer-specific ecosystem decisions with a neutral global ledger,
  consumer-owned scoped overlays, contextual conflicts, and complete
  direct-owner/dependency-path audit evidence.
- Add fully-generated ownership/recovery v2, native `model sync/check`, offline
  dependency audit/explain, splash diagnostics, and the modeling Codex skill.
- Scope unavoidable replacement/prohibition transitives to explicit approved
  direct owners without permitting the same package as a direct dependency.
- Add analyzer-AST scanning, Native Strict `DT1001`–`DT1015` parity, stable
  config v1, five blueprints, and complete MVVM slices.
- Initial read-only diagnostics and safe generators.
- Add the typed project service shared directly by the CLI and local MCP server.
- Make baseline replacement recoverable through stage, backup, journal, and
  rollback.
- Expand managed Codex sync from six to ten focused, implicitly invocable skills
  with typed templates, provider-specific references, and unchanged protection
  for modified or consumer-owned skills.
