name: dartitect
description: Native-first lifecycle, ownership, architecture events, and typed Result primitives.
version: 1.0.0-rc.9
repository: https://github.com/ftr-tuta/dartitect
issue_tracker: https://github.com/ftr-tuta/dartitect/issues
topics: [architecture, lifecycle, result, dart]
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  test: ^1.26.3
