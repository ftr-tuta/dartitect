name: dartitect_transfer
description: Provider-neutral resumable chunk transfer contracts and bounded orchestration for Dart.
version: 1.1.0-rc.3
publish_to: none
repository: https://github.com/ftr-tuta/dartitect
issue_tracker: https://github.com/ftr-tuta/dartitect/issues
topics: [transfer, upload, download, architecture]
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  dartitect:
    git:
      url: https://github.com/ftr-tuta/dartitect.git
      path: packages/dartitect
      tag_pattern: 'v{{version}}'
    version: 1.1.0-rc.3
dev_dependencies:
  test: ^1.26.3
