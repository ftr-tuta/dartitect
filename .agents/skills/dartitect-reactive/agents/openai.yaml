interface:
  display_name: "Dartitect Reactive"
  short_description: "Build hot, warm, and cold causal Flutter resources"
  default_prompt: "Use $dartitect-reactive to implement a causal reactive feature."
policy:
  allow_implicit_invocation: true
