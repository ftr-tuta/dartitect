interface:
  display_name: "Dartitect Adapters"
  short_description: "Wire provider SDKs at infrastructure composition"
  default_prompt: "Use $dartitect-adapters to integrate a Dartitect provider safely."
policy:
  allow_implicit_invocation: true
