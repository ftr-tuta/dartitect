interface:
  display_name: "Dartitect Testing"
  short_description: "Verify failure, lifecycle, provider, and leak contracts"
  default_prompt: "Use $dartitect-testing to design a Dartitect verification matrix."
policy:
  allow_implicit_invocation: true
