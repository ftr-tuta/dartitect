interface:
  display_name: "Dartitect Runtime"
  short_description: "Own Result, command, graph, and Flutter lifecycles"
  default_prompt: "Use $dartitect-runtime to implement an owned Dartitect runtime."
policy:
  allow_implicit_invocation: true
