interface:
  display_name: "Dartitect Offline First"
  short_description: "Build durable local authority, outbox, and sync"
  default_prompt: "Use $dartitect-offline-first to implement a durable local-first flow."
policy:
  allow_implicit_invocation: true
