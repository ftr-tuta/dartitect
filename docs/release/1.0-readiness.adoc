= Dartitect 1.0 readiness decision

Status:: GIT CONSUMPTION CANDIDATE ASSEMBLED; NOT READY FOR FORMAL RELEASE
Implementation status:: Code and the sixteen-package `1.0.0-rc.3` cohort are assembled; external root checks and native evidence remain pending
Decision date:: 2026-08-26 UTC
Current candidate cohort:: `1.0.0-rc.3`
Decision authority:: maintainer-delegated automation recorded in `tool/goal_gates.json`

== Decision

The public tree is the complete Git candidate for 16 packages and 17 public
entrypoints. Its consumption tag is annotated, unsigned, protected, and carries
no GitHub Release. Dependency overrides pin every selected and transitive
Dartitect package to the same repository/tag/commit. No package is authorized
for pub.dev publication.

This record does not claim formal `READY_FOR_1_0_RC` or `READY_FOR_1_0` status.
The technical-hardening work corrects the previously recorded
`OwnedRuntimeSlot.replaceGraph` ambiguity and its focused replacement/cleanup
matrix passes. That removes the semantic blocker but does not authorize this
cohort: the clean final candidate still needs same-SHA CI, Security, native,
hosted/Git canary, benchmark, and release-audit receipts.

== Root evidence boundary

The root SHA and tree cannot be written into their own tree. The private
checkpoint records them after the canonical commit is created. That exact SHA
must receive:

* Linux / Flutter 3.47.1 (floor);
* Linux / Flutter stable;
* Windows / Flutter 3.47.1;
* macOS and iOS / Flutter 3.47.1;
* OSV / merge candidate on push;
* trusted Security/OSV scanning;
* clean-clone release audit;
* Git consumption through an annotated tag and remote URL.

The candidate is not tagged until the pre-tag local audits and disposable bare-
remote canaries pass. After the public protected tag exists, the three canaries
must be repeated against `https://github.com/ftr-tuta/dartitect.git`.

== Remaining evidence

The native harness defines five candidate-bound cells. Android runtime behavior
is verified only on a physical Android 14/API 34 device through the isolated
ADB server; API 24 is build compatibility, not runtime evidence. iOS evidence
uses deployment-floor builds plus a dynamically selected CI simulator. No
Android emulator or physical iPhone is part of this candidate.

The five receipts, CI and Security runs, dry-runs, benchmarks, and Git canaries
must all refer to the same final `main` SHA. Until those receipts close V1S-04
and V1S-13, `rc_readiness_decision` remains `NOT_READY_FOR_1_0_RC`.

The formal signed-bundle/pub.dev path remains `NOT_AUTHORIZED`. Stable promotion
remains `NOT_ASSEMBLED` and its publication decision remains `NOT_AUTHORIZED`.

Signed-off-by: ftr <ftr@tuta.com>
