= Dartitect 1.0 Git candidate implementation status
:toc:

Snapshot:: 2026-08-29
Release status:: *ACTIONS READINESS PENDING*
Feature policy:: *FEATURE FREEZE*
Toolchain floor:: Dart 3.13 / Flutter 3.47.1 stable
Target topology:: 24 packages / 30 public entrypoints

`tool/goal_gates.json` is the machine-readable roadmap authority. GitHub
Actions is the only technical evidence authority. Local commands are optional
feedback and cannot declare an RC or stable candidate ready.

[cols="1,2,5",options="header"]
|===
| Goals | State | Evidence

| V1S-00–V1S-03
| IMPLEMENTED_LOCAL
| Roadmap, privacy/consumer-neutrality, ecosystem, generator recovery, media,
geometry, Drift, modular modeling/compiler, and public API guards are
implemented for the twenty-four-package topology.

| V1S-04
| IN_PROGRESS
| Pull requests, merge queue candidates, and `main` run the coordinated CI
graph. Branch protection requires only `CI / Required`; OSV is inside that graph.
The required graph also includes native Drift on all desktop hosts and a real
two-page Chrome Web job with portable and isolated header profiles.

| V1S-05–V1S-12
| IMPLEMENTED_LOCAL
| Lifecycle, reactive, model, capability, release, runtime, persistence, race,
benchmark, clean-clone, and canary gates are implemented.
The provider canary owns its Drift schema and exercises transaction/checkpoint
ports; the offline-first canary opens Drift and ObjectBox in separate bounded
contexts without dual-write.
The RC8 real vertical Task canary and thin-consumer canary cover target-aware
profiles, typed local extensions, capability-specific composition, credential
generations, durable operational contexts, explicit sync triggers, OpenAPI,
consumer-tax, semantic API compatibility, and observed runtime evidence with
zero residual resources.

| V1S-13
| IN_PROGRESS
| Schema v3 defines exactly five cells: Android API 24 build, Android API 34
hosted emulator, iOS 14 build, iOS 12 build, and the current hosted iOS
simulator. Every manifest binds SHA, tree, run ID, attempt, and hosted runner.

| V1S-14–V1S-17
| IN_PROGRESS
| A successful push to `main` produces one `actions-readiness-v1` artifact for
90 days with job conclusions and artifact hashes. RC validation consumes the
same nominal five native cells.

| V1-18
| IN_PROGRESS
| Manual publication accepts `source_sha`, `ci_run_id`, and `channel`, verifies
the successful main run and readiness hashes, records the actor, then performs
only the selected tag, GitHub Release, or pub.dev action.
|===

== Hosted-only invariant

No gate requires a phone, tablet, iPhone, USB connection, private ADB socket, or
privately managed runner. Android runtime uses a clean API 34 Google APIs AVD on
`ubuntu-24.04`; iOS runtime uses an installed simulator on `macos-15`. A boot or
simulator availability failure makes `CI / Required` fail.

== Permanent architecture invariants

* constructor injection and explicit owned/borrowed lifetime;
* local persistence remains presentation authority;
* consumer-owned retry, auth, schema, transaction, and conflict policy;
* dependency-free core and isolated optional capabilities;
* fresh graph construction and reverse teardown in every isolate.
