name: dartitect_flutter_quality_eval_fixture
description: Hermetic Flutter quality agent evaluation fixture.
version: 0.0.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
