name: dartitect_lints
description: Local-only bridge for the unpublished workspace plugin DAG.
version: 1.0.0-rc.5
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  analysis_server_plugin: ^0.3.18
  analyzer: ^13.3.0
  analyzer_plugin: ^0.14.12
  dartitect_modeling_analyzer:
    path: ../dartitect_modeling_analyzer
