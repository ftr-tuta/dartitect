# Dartitect model generator fixture

This workspace package commits the generated output and ownership manifest. It
must compile and test from a clean checkout without running the generator.
`dartitect model check --root examples/model_generator_fixture` verifies
freshness without writing.
