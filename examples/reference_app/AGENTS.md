# Dartitect reference app

Use constructor injection and public package entrypoints. Domain is pure Dart;
presentation must not import Dio/ObjectBox; routing and UI effects remain in
widgets. Validate with `flutter analyze`, `flutter test`, and `dartitect inspect`.
